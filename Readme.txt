To run this program follow the steps below: 

1) Download the project.
2) Open terminal cd to the project location.
3) Run npm install to dwonload the dependencies.
4) Run ng serve command to host the project locally.  
5) Open the porject via localhost.



Program description:

In shared service  getNestedDataSet() is used to convert json into a nested json structure using recurive call. 

Calling app component based on nested json child  to display nested list structure.
 